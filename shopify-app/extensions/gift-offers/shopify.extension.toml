api_version = "2024-10"

# Theme App Extension for Grounded Drops gift offers.
# This extension contains NO server code — it ships static assets + a Liquid
# "app embed" block that Shopify serves from its own CDN. Hosting cost: $0.
[[extensions]]
type = "theme"
name = "Grounded Drops Gift Offers"
handle = "gift-offers"
